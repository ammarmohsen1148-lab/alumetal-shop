<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Cairo', sans-serif; }</style>
</head>
<body class="bg-slate-800 min-h-screen flex items-center justify-center p-4">

    <div class="bg-white rounded-2xl shadow-2xl w-full max-w-lg overflow-hidden">
        <div class="bg-amber-500 p-6 text-center">
            <h2 class="text-2xl font-bold text-slate-900">إضافة مشروع جديد</h2>
        </div>
        
        <form action="/add-project" method="POST" enctype="multipart/form-data" class="p-8 space-y-5">
            <div>
                <label class="block text-gray-700 font-bold mb-2">عنوان العمل</label>
                <input type="text" name="title" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:border-amber-500" required>
            </div>

            <div>
                <label class="block text-gray-700 font-bold mb-2">القسم</label>
                <select name="category" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:border-amber-500">
                    <option value="مطبخ">مطبخ</option>
                    <option value="شباك">شباك</option>
                    <option value="باب">باب</option>
                    <option value="واجهة">واجهة</option>
                </select>
            </div>

            <div>
                <label class="block text-gray-700 font-bold mb-2">الوصف</label>
                <textarea name="description" rows="3" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:border-amber-500"></textarea>
            </div>

            <div>
                <label class="block text-gray-700 font-bold mb-2">الصور (اختر المجموعة كاملة)</label>
                <input type="file" name="photos" multiple class="w-full px-4 py-2 border rounded-lg bg-gray-50 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-amber-500 file:text-white hover:file:bg-amber-600" required>
            </div>

            <button type="submit" class="w-full bg-slate-900 text-white font-bold py-3 rounded-lg hover:bg-slate-800 transition-colors">
                نشر المشروع
            </button>
            
            <div class="text-center mt-4">
                <a href="/" class="text-gray-500 hover:text-slate-900 text-sm">العودة للموقع</a>
            </div>
        </form>
    </div>

</body>
</html>